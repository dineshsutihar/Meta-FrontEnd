// Concept of Array In JavaScript
var train =["Wheat","barley","red","green","yellow"];
function arrList(train){
    for(var i=0;i<train.length;i++){
        console.log(train[i]);
    }
    console.log("Length of array is "+train.length);
}
arrList(train);
console.log(train);

