let a=2;
let b=3;
console.log(a>b);

console.log(a<b);

console.log(a==b);

console.log(100=='100'); // == is used to compare the value only 
console.log(100==='100');// === is used to compare the value and data type

console.log(100!='100'); // != is used to compare the value only
console.log(100!=='100');// !== is used to compare the value and data type

/*
Logical AND operator: && 

Logical OR operator: || 

Logical NOT operator: ! 

The modulus operator: % 

The equality operator: ==

The strict equality operator: ===

The inequality operator: !=

The strict inequality operator: !==

The addition assignment operator: += 

The concatenation assignment operator: += (it's the same as the previous one - more on that later

 */